@model List<DGHCM.Models.FinanceBankMaster>
@{
    ViewBag.Title = "FinanceBankMasterIndex";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>

    body {
        background-image: url('../../content/images/office image.png');
        background-size: cover;
    }

    .card {
        background: #fff5;
        backdrop-filter: blur(7px);
        border-radius: 50%
    }

    .card-header {
        background: #ffffffa1;
    }



    .card-body {
        background: #fffb;
    }

    thead th {
        background: #d5d1defe;
    }
</style>
<div class="container">
    <div class="card">
        <div class="card-header  text-black d-flex flex-wrap justify-content-between align-items-center">

            <div>
                <h2 class="text-black m-0">Finance Bank</h2>
            </div>
            <div>
                <a href="~/Finance/FinanceBankMasterCreate" class="btn btn-transparent" data-toggle="modal"><i class="fa-solid fa-circle-plus fa-lg"></i><span>Add New</span></a>
            </div>
        </div>

        <div class="card-body ">
            <div class="container">
                <form class="main" name="Display">


                    @if (Model != null && Model.Count > 0)
                    {
                        <table class="table table-bordered table-striped table-hover mt-2">
                            <thead class="bg-secondary-light">
                                <tr align="center">
                                    <th scope="col">S No</th>
                                    <th scope="col">Bank Name</th>
                                    <th scope="col">Is Active</th>
                                    <th colspan="2">Edit</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{int i = 1;}
                                @foreach (var dr in Model)
                                {

                                    <tr>
                                        <td align="center">@i</td>
                                        <td hidden="hidden">@dr.BankId</td>
                                        <td>@dr.BankName</td>
                                        <td align="center">
                                            @if (dr.IsActive == true)
                                            {
                                                <span class="badge rounded-pill bg-success">Active</span>}
                                            else
                                            {
                                        <span class="badge rounded-pill bg-danger">Deactive</span>}
                                        </td>

                                        <td align="center">

                                            <a href="@Url.Action("FinanceBankMasterEdit", "Finance", new { Id = dr.BankId })"><i class="fa fa-edit text-primary fa-lg"></i></a>
                                            |
                                            <a href="@Url.Action("FinanceBankMasterDelete", "Finance", new { Id = dr.BankId })"><i class="fa fa-trash text-danger fa-lg"></i></a>

                                        </td>
                                    </tr>
                                    i++;
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <span> No records found!!</span>
                    }

                </form>
            </div>
        </div>
    </div>
</div>


