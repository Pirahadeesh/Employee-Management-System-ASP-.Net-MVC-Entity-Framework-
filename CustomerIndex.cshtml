@model List<DGHCM.Models.CustomerDetail>
@{
    ViewBag.Title = "CustomerIndex";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>

    body {
        background-image: url('../../content/images/office image.png');
        background-size: cover;
    }

    .card {
        background: #fff5;
        backdrop-filter: blur(7px);
        border-radius: 50%
    }

    .text-black {
        font-size: 25px;
    }

    .card-header {
        background: #ffffffa1;
        padding: 3px;
    }

    .card-body {
        background: #fffb;
    }

    thead th {
        background: #d5d1defe;
    }
</style>
<div class="container">
    <div class="card">
        <div class="card-header  text-black d-flex flex-wrap justify-content-between align-items-center">

            <div>
                <h2 class="text-black m-0">Customer Details</h2>
            </div>
            <div>
                <a href="~/Finance/CustomerCreate" class="btn btn-transparent" data-toggle="modal"><i class="fa-solid fa-circle-plus fa-lg"></i><span>Add New</span></a>
            </div>
        </div>

        <div class="card-body ">
            <div class="container">
                <form class="main" name="Display">


                    @if (Model != null && Model.Count > 0)
                    {
                        <table class="table table-bordered table-striped table-hover mt-2">
                            <thead class="bg-secondary-light">
                                <tr align="center">
                                    <th scope="col">S No</th>
                                    <th scope="col">Customer Code</th>
                                    <th scope="col">Customer Name</th>                                    
                                    <th scope="col">Is Active</th>
                                    <th colspan="2">Edit</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{int i = 1;}
                                @foreach (var dr in Model)
                                {

                                    <tr align="center">
                                        <td>@i</td>
                                        <td hidden="hidden">@dr.CustomerDetailsId</td>
                                        <td>@dr.CustomerCode</td>
                                        <td>@dr.CustomerName</td>
                                        <td>
                                            @if (dr.IsActive == true)
                                            {
                                                <span class="badge rounded-pill bg-success">InActive</span>}
                                            else
                                            {
                                                <span class="badge rounded-pill bg-danger">Deactive</span>}
                                        </td>

                                        <td>

                                            <a href="@Url.Action("CustomerEdit", "Finance", new { Id = dr.CustomerDetailsId })"><i class="fa fa-edit text-primary fa-lg"></i></a>
                                            |
                                            <a href="@Url.Action("CustomerDelete", "Finance", new { Id = dr.CustomerDetailsId })"><i class="fa fa-trash text-danger fa-lg"></i></a>

                                        </td>
                                    </tr>
                                    i++;
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <span> No records found!!</span>
                    }

                </form>
            </div>
        </div>
    </div>
</div>


